<%= '<h2>'.html_safe %><%= link_to l(:label_public_keys), public_keys_path %> <%= '&#187;'.html_safe %> <%=l(:label_public_key_new)%><%= '</h2>'.html_safe %>

<%= error_messages_for @gitosis_public_key %>

<%= labelled_form_for :public_key, @gitosis_public_key, :url => { :action => "create" },
                          :lang => current_language do |f| %>

	<%= render :partial => 'form', :locals => { :f => f } %>
	<%= submit_tag l(:button_create) %>
<% end %>


<% html_title(l(:label_public_keys)) -%>
